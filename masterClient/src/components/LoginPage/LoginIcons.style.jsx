import { IconButton } from '@mui/material';
import { styled } from '@mui/material/styles';

export const LoginIcon = styled(IconButton)({
  width: '65px',
  height: '65px',
  color: 'white',
  border: '3px solid #1F1F53',
  borderRadius: '50%',
  padding: '10px',
});
